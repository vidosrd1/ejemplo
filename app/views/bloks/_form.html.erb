<%= form_with(model: blok) do |form| %>
  <% if blok.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(blok.errors.count, "error") %> prohibited this blok from being saved:</h2>

      <ul>
        <% blok.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
